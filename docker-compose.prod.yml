version: "3"

services:
  nginx:
    ports:
      - "3000:3000"
    build:
      context: .
      dockerfile: config/nginx/Dockerfile
    volumes:
      - sharedvolume:/billshare/
  db:
    image: postgres
    env_file:
      - env/postgres.env
  app:
    restart: always
    env_file:
      - env/prod.env
    build:
      context: .
      dockerfile: config/app/Dockerfile
    depends_on:
      - nginx
      - db
    volumes:
      - sharedvolume:/billshare/

volumes:
  sharedvolume: {}
